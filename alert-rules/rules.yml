groups:
- name: MesosAlerts
  rules:
    - alert: Test_Mesos_Alert
      expr: sum(mesos_master_uptime_secs) > 0
      for: 1m
      labels:
         severity: high
      annotations:
         description: 'test alert that will fire'
